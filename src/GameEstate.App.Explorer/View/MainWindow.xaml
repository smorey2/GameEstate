<Window x:Class="GameEstate.Explorer.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GameEstate.Explorer.View"
        xmlns:scenes="clr-namespace:GameEstate.Explorer.View"
        mc:Ignorable="d"
        Title="GameEstate Explorer" Width="1342" Height="798">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="20" />
            <RowDefinition Height=".1*" />
            <RowDefinition Height="63" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition MinWidth="250" Width="250" />
            <ColumnDefinition Width="20" />
            <ColumnDefinition Width=".1*" />
        </Grid.ColumnDefinitions>

        <local:MainMenu x:Name="MainMenu" Grid.Row="0" Grid.ColumnSpan="3"/>

        <TabControl x:Name="TabControl" Grid.Row="1" Margin="0,0,0,10" >
            <TabItem Header="Resources">
                <local:FileExplorer x:Name="PakFileExplorer" Margin="0,0,10,10" />
            </TabItem>
            <TabItem Header="Data">
                <local:FileExplorer x:Name="DatFileExplorer" Margin="0,0,0,10" />
            </TabItem>
            <TabItem Header="Engine">
                <ScrollViewer>
                    <TextBlock TextWrapping="Wrap">
                    Leverage agile frameworks to provide a robust synopsis for high level overviews. Iterative approaches to corporate strategy foster collaborative thinking to further the overall value proposition. Organically grow the holistic world view of disruptive innovation via workplace diversity and empowerment.
                    <LineBreak /><LineBreak />
                    Bring to the table win-win survival strategies to ensure proactive domination. At the end of the day, going forward, a new normal that has evolved from generation X is on the runway heading towards a streamlined cloud solution. User generated content in real-time will have multiple touchpoints for offshoring.
                    </TextBlock>
                </ScrollViewer>
            </TabItem>
        </TabControl>

        <GridSplitter Grid.Row="1" Grid.Column="1" Width="5" HorizontalAlignment="Center" />

        <scenes:EngineView Grid.Row="1" Grid.Column="2" Margin="0,22,10,10" />

        <TextBox Name="Status" Grid.Row="2" Grid.ColumnSpan="3" Margin="7.5,0,7.5,7.5" Padding="3"
                 TextWrapping="Wrap" 
                 ScrollViewer.VerticalScrollBarVisibility="Auto" />

    </Grid>
</Window>
